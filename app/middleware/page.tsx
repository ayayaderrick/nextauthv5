import React from "react";

const Middleware = () => {
  return (
    <main className="mt-8">
      <h1 className="text-4xl font-bold">Middleware usage</h1>
      <p className="text-lg mt-4">
        This page is protected by using the universal auth() method in Next.js
        Middleware .
      </p>
    </main>
  );
};

export default Middleware;
